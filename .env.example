# Environment Configuration for RAG Pipeline

# ===================================
# Core Configuration
# ===================================

# Data paths
DATA_PATH=./data
CHROMA_PATH=./data/chroma_db

# API Keys
MISTRAL_API_KEY=your_mistral_api_key_here

# ===================================
# Intent Classification
# ===================================

# Confidence threshold for domain routing (0.0-1.0)
# Queries below this threshold are routed to chitchat handler
INTENT_THRESHOLD=0.30

# ===================================
# Hybrid Retrieval
# ===================================

# Vector vs keyword weight (should sum to ~1.0)
HYBRID_VECTOR_WEIGHT=0.60

# ===================================
# Score Gate
# ===================================

# Minimum relevance threshold (0.0-1.0)
# Results below this threshold are filtered out
SCORE_GATE_THRESHOLD=0.20

# ===================================
# CrossEncoder Reranking
# ===================================

# Enable/disable CrossEncoder (true/false)
# Note: Adds ~100ms latency but improves precision
CROSS_ENCODER_ENABLED=true

# ===================================
# PII Redaction
# ===================================

# Enable/disable PII redaction (true/false)
# Requires spaCy: pip install spacy && python -m spacy download en_core_web_sm
PII_REDACTION_ENABLED=false

# ===================================
# Logging & Monitoring
# ===================================

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# ===================================
# Production Recommendations
# ===================================

# For POC/Development:
# - CROSS_ENCODER_ENABLED=false (faster, good enough)
# - PII_REDACTION_ENABLED=false (not needed for material pricing)
# - INTENT_THRESHOLD=0.30 (balanced)
# - SCORE_GATE_THRESHOLD=0.20 (20% minimum)

# For Production:
# - CROSS_ENCODER_ENABLED=true (best quality)
# - PII_REDACTION_ENABLED=true (if handling sensitive data)
# - INTENT_THRESHOLD=0.25 (slightly more aggressive routing)
# - SCORE_GATE_THRESHOLD=0.15-0.25 (tune based on precision/recall)

# For Low-Resource Environments:
# - CROSS_ENCODER_ENABLED=false
# - PII_REDACTION_ENABLED=false
# - Consider using Ollama fallback only
